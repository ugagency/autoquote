# REMOVA esta linha:
# version: '3.8'

services:
  autoquote-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autoquote-app
    ports:
      - "5020:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=AutoQuote-Secret-Key-2024-Muito-Longa-32-Chars
      - SUPABASE_URL=https://rzglsaargcpwewrgzwtx.supabase.co
      - SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ6Z2xzYWFyZ2Nwd2V3cmd6d3R4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NzY4NTA2NCwiZXhwIjoyMDgzMjYxMDY0fQ.A-1z2X-YdqEA083Kbso4kSnkbJPngHRR6dTiGvnM9p0
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ./data:/app/data
      - ./output:/app/output
    networks:
      - autoquote-network
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:5000/" ]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  autoquote-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local
